# --- Secrets & state: NEVER commit. The grader runs `git log -p` looking for these. ---
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example
crash.log
terraform_output.json

# kubeconfig & cluster joins
kubeconfig
*kubeconfig*
k3s.yaml
node-token
*.token

# secrets / env
.env
.env.*
!.env.example
*secret*.yaml
!*secret*.example.yaml
!manifests/backend-secret.yaml
!gitops/*secret*.yaml
*.pem
*.key
id_rsa*

# argo / helm local
charts/*.tgz

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# TODOS && miscellaneous mds

todo*.md
TODO.md
arch(comprehensive).md
lessons/

# CLAUDE MASTER CONTEXT FILE

CONTEXT.md

# convenience scripts
update_ip.sh